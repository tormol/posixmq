task:
  name: FreeBSD 11.2 amd64 stable
  freebsd_instance:
    image: freebsd-11-2-release-amd64
    cpu: 1
    memory: 1
  env:
    RUST_BACKTRACE: 1
  setup_script:
    - pkg install -y curl
    - curl https://sh.rustup.rs -sSf --output rustup.sh
    - sh rustup.sh -y
  cargo_cache:
    folder: $HOME/.cargo/registry
    fingerprint_script: cat Cargo.lock 2> /dev/null || true
  build_script:
    - . $HOME/.cargo/env
    - cargo build
    - cargo build --features mio
  test_script:
    - . $HOME/.cargo/env
    - kldload mqueuefs
    - cargo test --features mio
    - cargo test --features mio -- --ignored --test-threads 1
    - cargo run --example limits
  before_cache_script:
    - rm -rf $HOME/.cargo/registry/index

task:
  name: Linux amd64 1.31
  container:
    image: rust:1.31
    cpu: 1
    memory: 1
  cargo_cache:
    folder: $HOME/.cargo/registry
    fingerprint_script: cat Cargo.lock 2> /dev/null || true
  build_script:
    - cargo build
    - cargo build --features mio
  test_script:
    - cargo test --features mio
    - cargo test --features mio -- --ignored --test-threads 1
    - cargo run --example limits
  before_cache_script:
    - rm -rf $HOME/.cargo/registry/index

task:
  name: Linux amd64 nightly
  container:
    image: rustlang/rust:nightly
    cpu: 1
    memory: 1
  allow_failures: false
  cargo_cache:
    folder: $HOME/.cargo/registry
    fingerprint_script: cat Cargo.lock 2> /dev/null || true
  build_script:
    - cargo build -Z minimal-versions
    - cargo build -Z minimal-versions --features mio
  test_script:
    - cargo test -Z minimal-versions --features mio
  before_cache_script:
    - rm -rf $HOME/.cargo/registry/index

task:
  name: Cross-check stable
  container:
    image: rust:latest
    cpu: 2
    memory: 1
  allow_failures: false
  cargo_cache:
    folder: $HOME/.cargo/registry
    fingerprint_script: cat Cargo.lock 2> /dev/null || true
  build_script:
    - rustup target add x86_64-unknown-netbsd
    - cargo check --target x86_64-unknown-netbsd --features mio --tests
    - rustup target add i686-unknown-linux-gnu
    - cargo check --target i686-unknown-linux-gnu --features mio --tests
    - rustup target add x86_64-unknown-linux-gnux32
    - cargo check --target x86_64-unknown-linux-gnux32 --features mio --tests
    - rustup target add aarch64-unknown-linux-gnu
    - cargo check --target aarch64-unknown-linux-gnu --features mio --tests
  before_cache_script:
    - rm -rf $HOME/.cargo/registry/index
